import { RunningPaceFormatPipe } from './running-pace-format.pipe';

describe('RunningPaceFormatPipe', () => {
	it('create an instance', () => {
		const pipe = new RunningPaceFormatPipe();
		expect(pipe).toBeTruthy();
	});
});
